# CLAUDE.md — 개발 지침

이 프로젝트에서 코딩할 때 **상시 지켜야 하는 규칙**과 참고 문서 링크. (게임 규칙은 여기 없음 → 아래 링크)

## 📚 참고 문서 (필요할 때 즉시 읽어 맥락 유지)

| 문서 | 무엇 | 언제 읽나 |
|---|---|---|
| [`docs/GAME-DESIGN.md`](docs/GAME-DESIGN.md) | **게임 시스템 명세 (SoT).** 모든 규칙·수치·설계 결정 | 게임 로직을 만들거나 바꿀 때 |
| [`docs/CODE-MAP.md`](docs/CODE-MAP.md) | **코드 매핑.** 어떤 파일/함수가 무슨 역할인지 | 작업 시작 전, 어디를 고칠지 찾을 때 |
| [`README.md`](README.md) | 실행법·아키텍처 요약 | 돌려볼 때 |

작업 시작 전: **CODE-MAP을 먼저 훑어** 해당 부분이 어디인지 잡고, 게임 규칙은 GAME-DESIGN을 진실로 삼는다.

## 🔒 불변 규칙 (어기면 안 됨)

1. **결정론 신성불가침** (GAME-DESIGN 8.3): `src/core/`에서 `Math.random()`·`Date.now()`·`new Date()` **금지.** 모든 무작위는 `state.rng`(`Rng`)에서만. → 리플레이·테스트·AI 재현성의 근간.
2. **코어 순수성** (8.1): `src/core/`는 렌더링/IO 의존 **0**. `console.*`·DOM·readline·파일 IO 금지. 출력/입력은 `src/cli/`·`src/web/`에서만.
3. **데이터 주도** (8.6): 스킬·상태이상·캐릭터·전투배치·포메이션 보너스 등 **콘텐츠는 `src/data/`에 데이터로.** 엔진(`engine.ts`)에 콘텐츠를 하드코딩하지 말 것. 원칙: **메커니즘=엔진, 값=데이터.**
4. **모든 상태 변화는 이벤트 로그로** (8.3/8.5): 상태를 바꾸면 대응하는 `GameEvent`를 `state.log`에 push. 이벤트가 리플레이·애니메이션의 구동원. 새 메커니즘 → 새 이벤트 타입 추가.
5. **관측에 모든 결정 정보** (8.2): AI/사람이 결정에 필요한 건 전부 `buildObservation()`에 노출(픽셀/숨은 값에 정보 두지 않기). 수치는 `원본→현재` 투명 병기(0.2).
6. **설계와 코드 동기화**: 구현하다 게임 규칙을 정하거나 바꾸면 **GAME-DESIGN.md를 갱신**(상태표시 포함). 말없이 다르게 구현하지 말 것.

## ⚙️ 런타임/문법 제약 (Node 24 네이티브 TS)

- **Node ≥ 24**가 `.ts`를 직접 실행(타입 스트리핑) → **빌드 스텝 없음.**
- 따라서 **erasable TypeScript만**: `enum`·`namespace`·파라미터 프로퍼티(`constructor(private x)`)·데코레이터·`import =` **금지**. `type`/`interface`/`as`/제네릭/`as const`로 표현.
- 로컬 import는 **확장자 `.ts` 명시** (`import { x } from "./engine.ts"`).
- 타입 import는 `import type` 사용.

## 🔀 Git 워크플로우

- 기본 브랜치 = **`main`**, 원격 = **`origin`** (github.com/Yoonkyu-Lee/sundaypudding-roguelike).
- **의미 있는 슬라이스를 완료하면 자동으로 커밋한다** — 간결한 한 줄 커밋 메시지. (체크리스트 참조)
- **푸시는 사용자가 명시적으로 지시할 때만.** 임의 push 금지.
- **분기 구현·시행착오·실험이 필요하면 브랜치를 따서 작업하고, 끝나면 `main`에 merge.** 안정적·확정된 슬라이스는 `main`에 직접 커밋해도 됨.
- 커밋 메시지: 한 줄, 무엇을 했는지. (예: `feat: 포메이션 열보너스(총량보존) 구현`, `test: 끼어들기 결정론 테스트 추가`)

## 🧪 명령어

```bash
npm run demo        # 자동플레이 1판 (디버깅/감각 확인)
npm run play        # 대화형 플레이
npm test            # 단위 테스트 (결정론 포함) — 반드시 green 유지
npm run typecheck   # tsc --noEmit — 0 에러 유지
```

## ✅ "의미 있는 슬라이스 완료" 체크리스트

새 기능/슬라이스를 끝냈다고 판단하면 **반드시**:

1. [ ] `npm test` 통과 (결정론 테스트 포함). 새 메커니즘엔 **결정론 단위 테스트 추가.**
2. [ ] `npm run typecheck` 0 에러.
3. [ ] **`docs/CODE-MAP.md` 갱신** — 새 파일/함수/기능 위치를 매핑에 반영.
4. [ ] **`docs/GAME-DESIGN.md` 갱신** — 구현 상태(8.7)·새로 확정된 규칙 반영.
5. [ ] 필요 시 `README.md`(실행법) 갱신.
6. [ ] **커밋** (간결한 한 줄 메시지). 실험성 작업이었다면 브랜치에서 → `main`에 merge. **푸시는 사용자 지시 시에만.**

> 코드 식별자·주석의 기술 용어는 영어 가능하나, 도메인/설계 용어는 GAME-DESIGN의 한글 도메인 용어(예: 끼어들기/쉴드/숙련도)를 따른다.
