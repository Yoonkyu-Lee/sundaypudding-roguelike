# CLAUDE.md — 개발 지침

이 프로젝트에서 코딩할 때 **상시 지켜야 하는 규칙**과 참고 문서 링크. (게임 규칙은 여기 없음 → 아래 링크)

## 📚 참고 문서 (필요할 때 즉시 읽어 맥락 유지)

| 문서 | 무엇 | 언제 읽나 |
|---|---|---|
| [`docs/GAME-DESIGN.md`](docs/GAME-DESIGN.md) | **게임 시스템 명세 (SoT).** 모든 규칙·수치·설계 결정 | 게임 로직을 만들거나 바꿀 때 |
| [`docs/CODE-MAP.md`](docs/CODE-MAP.md) | **코드 매핑.** 어떤 파일/함수가 무슨 역할인지 | 작업 시작 전, 어디를 고칠지 찾을 때 |
| [`docs/UI-GLOSSARY.md`](docs/UI-GLOSSARY.md) | **GUI 도메인 명칭 (SoT).** 화면·패널·모달의 통일 이름 ↔ 코드 매핑 | GUI를 만들거나 부를 때 (이 이름으로 통일) |
| [`README.md`](README.md) | 실행법·아키텍처 요약 | 돌려볼 때 |

작업 시작 전: **CODE-MAP을 먼저 훑어** 해당 부분이 어디인지 잡고, 게임 규칙은 GAME-DESIGN을 진실로 삼는다.

## 🔒 불변 규칙 (어기면 안 됨)

1. **결정론 신성불가침** (GAME-DESIGN 8.3): `src/core/`에서 `Math.random()`·`Date.now()`·`new Date()` **금지.** 모든 무작위는 `state.rng`(`Rng`)에서만. → 리플레이·테스트·AI 재현성의 근간.
2. **코어 순수성** (8.1): `src/core/`는 렌더링/IO 의존 **0**. `console.*`·DOM·readline·파일 IO 금지. 출력/입력은 `src/cli/`·`src/web/`에서만.
3. **데이터 주도** (8.6/8.8): 스킬·상태이상·캐릭터·전투배치·노드 적구성·포메이션 보너스 등 **콘텐츠는 `src/data/`에 데이터로.** 엔진(`src/core/`)에 콘텐츠를 하드코딩하지 말 것. 원칙: **메커니즘=엔진, 값=데이터.** (경계·프로토콜은 아래 전용 섹션 참조)
4. **모든 상태 변화는 이벤트 로그로** (8.3/8.5): 상태를 바꾸면 대응하는 `GameEvent`를 `state.log`에 push. 이벤트가 리플레이·애니메이션의 구동원. 새 메커니즘 → 새 이벤트 타입 추가.
5. **관측에 모든 결정 정보** (8.2): AI/사람이 결정에 필요한 건 전부 `buildObservation()`에 노출(픽셀/숨은 값에 정보 두지 않기). 수치는 `원본→현재` 투명 병기(0.2).
6. **설계와 코드 동기화**: 구현하다 게임 규칙을 정하거나 바꾸면 **GAME-DESIGN.md를 갱신**(상태표시 포함). 말없이 다르게 구현하지 말 것.

## 🧩 모듈 구조 & 파일 분리 (필수 습관)

한 파일에 코드가 뭉치면 못 읽고 못 고친다. **미리미리 분리한다.**

- **1 모듈 = 1 책임.** 서브시스템은 **폴더 + `index.ts` 배럴**로 공개 API를 노출, 소비자는 **배럴만 import**(내부 파일 직접 참조 금지).
- **소프트캡 ~300줄.** 권장 한도. 한 파일이 ~400줄을 넘기 직전이면 **먼저 분리하고** 기능을 추가한다(분리 후 추가, 추가 후 방치 X).
- **단방향 의존:** `data → types ← core → views(cli/web)`. core는 view를 import하지 않고, view는 core를 읽기만. 사이클이 생기면 공유 부분을 leaf 모듈(예: `core/util.ts`)로 빼낸다.
- **저위험 마이그레이션:** 기존 진입 파일은 **얇은 배럴(파사드)**로 남겨 내부만 분할 → 외부 import 경로 변경 최소화. (현 구조: `core/engine.ts`→`combat/*`, `core/run.ts`→`run/*`, `core/types.ts`→`types/{content,runtime}`.)
- 전체 모듈 트리는 **`docs/CODE-MAP.md`**가 SoT.

## 🎯 데이터 vs 엔진 경계 + 프리미티브-갭 프로토콜 (필수·명시)

**무엇이 디자이너의 '데이터'이고 무엇이 엔지니어의 '엔진'인지** 매 기능마다 명시적으로 판단한다 (상세: GAME-DESIGN 8.8).

- **데이터**(디자이너) = `src/data/*` + `core/types/content.ts` 스키마로 표현되는 콘텐츠. **엔진**(엔지니어) = `src/core/*`의 해석 메커니즘 = **프리미티브**.
- **프리미티브 카탈로그**(현재): SkillEffect 종류 · StatusDef 거동 필드 · AreaShape 종류 · Skill 필드(cooldown/accuracy/타겟마스크/reach/area/grantsInterrupt(To)/alwaysHit) · TurnOrderResolver(라운드제) · 런 노드 해소. (목록은 GAME-DESIGN 8.8 유지)
- **결정 프로토콜**: 새 기능 → ① 기존 프리미티브 조합으로 **원자적 표현 가능?** → ② 가능 = **데이터-온리**(`src/data`만) → ③ 불가 = **프리미티브 갭** → 엔진 변경 필요, **구현 전 사용자에게 형식대로 보고·승인**:
  > "이 기능은 새 엔진 프리미티브가 필요합니다 — **[무엇]**. 기존 [목록]으로는 원자적으로 불가(이유). 추가 위치: `types/content.ts` + `core/<module>`. 데이터-온리가 아닙니다."
- **엔진 프리미티브를 말없이 늘리지 않는다.** 커밋/보고에 **`[데이터-온리]` 또는 `[엔진 프리미티브 추가]`** 분류를 명시.

## ⚙️ 런타임/문법 제약 (Node 24 네이티브 TS)

- **Node ≥ 24**가 `.ts`를 직접 실행(타입 스트리핑) → **빌드 스텝 없음.**
- 따라서 **erasable TypeScript만**: `enum`·`namespace`·파라미터 프로퍼티(`constructor(private x)`)·데코레이터·`import =` **금지**. `type`/`interface`/`as`/제네릭/`as const`로 표현.
- 로컬 import는 **확장자 `.ts` 명시** (`import { x } from "./engine.ts"`).
- 타입 import는 `import type` 사용.

## 🔀 Git 워크플로우

- 기본 브랜치 = **`main`**, 원격 = **`origin`** (github.com/Yoonkyu-Lee/sundaypudding-roguelike).
- **의미 있는 슬라이스를 완료하면 자동으로 커밋한다** — 간결한 한 줄 커밋 메시지. (체크리스트 참조)
- **푸시는 사용자가 명시적으로 지시할 때만.** 임의 push 금지.
- **분기 구현·시행착오·실험이 필요하면 브랜치를 따서 작업하고, 끝나면 `main`에 merge.** 안정적·확정된 슬라이스는 `main`에 직접 커밋해도 됨.
- 커밋 메시지: 한 줄, 무엇을 했는지. (예: `feat: 포메이션 열보너스(총량보존) 구현`, `test: 끼어들기 결정론 테스트 추가`)

## 🔁 백엔드/프론트엔드 평행 개발 (필수)

- **슬라이스마다 코어(백엔드)와 웹(프론트엔드)을 평행하게 개발한다.** 플레이어가 마주하는 기능이면 둘 다 같이 간다.
- **CLI/터미널/저수준 흐름 = 개발자·AI의 "게임 로직 검증"용** (테스트·`npm run demo`). 플레이어 UX가 아니다.
- **웹 = 플레이어용 GUI. 품질 기준 = 현재 수준**: 2단계 타겟팅(스킬→타겟), 칸 하이라이트(2.4), 머리위 명중%(2.7), 눈금 화살표, 호버 HP 미리보기(0.2), 이벤트 로그 재생(8.5). 새 메커니즘은 **이 수준의 시각 어포던스**로 표현.
- 단, 코어의 generic 뷰(상태칩·HP바 등)가 이미 커버하면 프론트 추가작업은 최소로 — **불필요한 UI를 만들지 말 것.** 새 개념이 새 시각표현을 요구할 때만.
- 검증: 로직은 결정론 테스트, 플레이어 표면은 `npm run dev`(HMR)로 라이브 확인.

## 🧪 명령어

```bash
npm run demo        # 자동플레이 1판 (디버깅/감각 확인)
npm run play        # 대화형 플레이
npm test            # 단위 테스트 (결정론 포함) — 반드시 green 유지
npm run typecheck   # tsc --noEmit — 0 에러 유지
npm run check       # ★ 자동 게이트: 줄수캡·코어순수성·배럴·tsc·test·데모회귀 (커밋 전 자동 실행)
```

> **드리프트 예방 = 점검을 "행동하는 순간"으로**. 슬라이스 **진입 시 `/slice-plan`**(데이터/엔진 분류·프리미티브 갭·모듈·웹·크기 명시), **종료 시 `/slice-wrap`**(게이트+문서+커밋). 기계적 검사는 `npm run check`가 강제(git pre-commit 훅이 자동 실행 — 활성화: `git config core.hooksPath .githooks`).

## ✅ "의미 있는 슬라이스 완료" 체크리스트

새 기능/슬라이스를 끝냈다고 판단하면 **반드시** (이 리스트는 `/slice-wrap`이 구동):

1. [ ] `npm run check` 통과 (test+typecheck+줄수캡+코어순수성+배럴+데모회귀 한 번에). 새 메커니즘엔 **결정론 단위 테스트 추가.**
2. [ ] (1에 typecheck 포함 — 개별 확인 필요 시 `npm run typecheck`)
3. [ ] **파일 분리 점검** — 캡(~300줄) 근접/초과 파일이 생겼으면 책임별로 분리(배럴 유지). 새 기능이 캡을 넘기면 먼저 분리 후 추가.
4. [ ] **데이터/엔진 분류 명시** — 이번 변경이 `[데이터-온리]`인지 `[엔진 프리미티브 추가]`인지 커밋 메시지/보고에 기록. 프리미티브 갭이면 사전 승인 받았는지 확인.
5. [ ] **`docs/CODE-MAP.md` 갱신** — 새 파일/함수/기능 위치를 매핑에 반영.
6. [ ] **`docs/GAME-DESIGN.md` 갱신** — 구현 상태(8.7)·새로 확정된 규칙 반영.
7. [ ] 필요 시 `README.md`(실행법) 갱신.
8. [ ] **프론트엔드 평행 반영** — 플레이어 표면이 있으면 `src/web`도 갱신하고 `npm run dev`로 확인 (generic 뷰가 커버하면 생략 가능, 이유 기록).
9. [ ] **커밋** (간결한 한 줄 메시지). 실험성 작업이었다면 브랜치에서 → `main`에 merge. **푸시는 사용자 지시 시에만.**

> 코드 식별자·주석의 기술 용어는 영어 가능하나, 도메인/설계 용어는 GAME-DESIGN의 한글 도메인 용어(예: 끼어들기/쉴드/숙련도)를 따른다.
