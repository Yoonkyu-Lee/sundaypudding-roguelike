{
  "name": "sundaypudding-roguelike",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "육성형 로그라이크 자체 엔진 — Rust 결정론 코어(rust/spr-core) + 웹 프론트(Tauri). 설계 SoT: docs/GAME-DESIGN.md",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test",
    "data:export": "node scripts/export-data.ts",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.web.json",
    "check": "node scripts/check.ts",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
